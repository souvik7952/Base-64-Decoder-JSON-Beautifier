# Base-64-Decoder-JSON-Beautifier
A lightweight, single-file HTML Base64 Decoder tool with a clean UI. Paste or upload encoded data to decode instantly, view formatted output, beautify JSON, and download results as TXT or JSON. Includes copy, clear, and error handling—no external libraries needed.
